<ul class="form_images">
  <%= Icon.all.each do |icon| %>
    <li> <%= link_to(image_tag(icon.icon_url, :title => icon.name, :border => 0, :alt => icon.name), icon.icon_url, :class => "fancy", :rel =>"group") %> </li>
  <% end %>
</ul>
