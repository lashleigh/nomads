<ul class="form_images">
  <% @icons do |icon| %>
    <li> <%= link_to(image_tag(icon.marker_url, :title => icon.name, :border => 0, :alt => icon.name), icon.marker_url, :class => "fancy", :rel =>"group") %> </li>
  <% end %>
</ul>
