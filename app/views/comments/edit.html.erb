<h1>Editing comment</h1>

<% form_for(@comment, :url => { :action => :update, :id => @comment.id }) do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit 'Save' %>
  </p>
<% end %>
